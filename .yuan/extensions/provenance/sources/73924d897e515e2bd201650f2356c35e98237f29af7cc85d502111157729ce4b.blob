---
name: test-integrity
description: 测试完整性与反作弊规范（Hard Gate 全绿必须"没被作弊"才可信）
spec_type: rule
version: "1.0.0"
---

# 测试完整性与反作弊规范（Test Integrity / Anti-Gaming）

> **来源**：参考 MVP 开发专家团的 test-integrity-anti-gaming 规范。
> **目的**：YuanForge 的 Tester 是 🟡 Hard Gate（全量测试 PASS 才交付），但"通过测试"本身可被游戏化——改松测试比改对代码省力。本规则给出**作弊目录 + 完整性差异 + 留出集**三道闸，让"全绿"在确认没作弊后才可信。
> **与 YuanForge 的衔接**：与 `verdict-protocol.md`（谁来裁、怎么判）互补——本规则管"凭什么信这套绿测"，verdict-protocol 管"结论怎么结构化回传"。Tester 合约的「反作弊检查」小节引用本规则。

---

## 一句话原则

**只要"通过"是靠改松验证而非改对代码换来的，即为作弊，等同未完成。**

---

## 作弊行为目录（出现即不合格）

| 作弊形态 | 具体表现 |
|----------|----------|
| 硬编码输出 / 特判可见用例 | 直接 return 期望值、只让"看得见的样例"过、逻辑对一般情况错 |
| 弱化 / 删断言 | assertEqual→assertTrue、放宽容差、删关键断言，缺陷出现不再变红 |
| skip / xfail / 注释静音 | 给碍事用例打 skip/xfail/ignore，失败被藏起、覆盖面缩水 |
| 篡改测试框架 | 改 conftest/setup/全局 fixture/断言钩子让失败被吞 |
| 提前 exit / 吞异常 | 校验前 return、try…except: pass，错误路径被绕过 |
| 改基线 / golden 文件 | 把快照/golden 改成当前（错误）输出，反向利用回归保护 |
| 放宽门禁 | 调低覆盖率阈值、关必过检查、加 lint 豁免，"拆门再宣称达标" |

---

## 验证手段（让作弊在结构上失效）

1. **作者改不动的确定性校验**：核心验收用一条实现/测试作者无权修改的确定性检查把关（独立 runner、固定输入→固定期望、产物级断言）。
2. **留出集（held-out）**：保留一组作者**不可见**的输入/期望用于最终判定；专门特判可见用例的实现会在这里崩。
3. **完整性差异（integrity diff）**：每次改动**单独审视对测试与门禁的改动**——断言被加强还是被删/弱化？是否新增 skip/xfail？是否动框架/阈值/豁免？**实现 diff 与测试 diff 必须分开评审**，不混在大 diff 里蒙混过关。
4. **独立检查**：验证由不写该实现的视角执行（verifier-critic），避免"自证清白"。

---

## 完整性差异：必须逐项盘问的改动信号

| 信号 | 默认判定 | 放行条件 |
|------|----------|----------|
| 断言被删除或放宽 | 可疑 | 明确理由且不降低区分力 + 评审签字 |
| 新增 skip / xfail / ignore | 可疑 | 关联到已登记真实缺陷与期限 |
| 改动测试框架 / 全局 fixture / 钩子 | 高危 | 独立复核，确认不吞失败 |
| 修改快照 / golden / 基线 | 可疑 | 确认是预期行为变化而非掩盖回归 |
| 调低覆盖率 / 加 lint 豁免 / 关必过检查 | 高危 | 默认拒绝，需显式治理审批 |
| 实现里特判 / 硬编码测试输入 | 作弊 | 不放行 |

---

## 反模式（出现即不合格）

1. 实现里特判/硬编码测试输入（只在背答案）
2. 靠删/弱化断言换绿（缺陷不再变红）
3. 用 skip/xfail/注释静音失败
4. 篡改测试框架吞掉失败（整套套件可信度坍塌）
5. 把实现和测试改动混进一个大 diff（完整性差异无法审）
6. 改门禁阈值/加豁免来"通过"（拆门再宣称达标）
7. 无留出集（只用可见用例判定，特判式实现永远过关）
8. 验证者就是实现者（自证清白，作弊无人戳穿）

---

## 最低交付 checklist（Tester 反作弊检查用）

- [ ] 核心验收由一道实现/测试作者**改不动**的确定性校验把关
- [ ] 存在作者不可见的留出（held-out）用例用于最终判定
- [ ] 实现 diff 与测试/门禁 diff **分离呈现并分别评审**（完整性差异）
- [ ] 任何删/弱断言、新增 skip/xfail、改框架/阈值/基线都有理由并经复核
- [ ] 验证由不写该实现的独立视角执行，给出 pass/fail + 证据
- [ ] 覆盖率阈值与必过检查不可被本次改动私自调低或豁免
- [ ] 被识破的作弊手法沉淀为项目级硬约束，防止重现

> *本规则与 `verdict-protocol.md` 的"过度设计护栏"一致：审查官只标正确性缺陷/需求未满足/契约安全破坏三类阻断，但**反作弊是这三类之上的硬底线**——绿测若被游戏化，等于没有 Gate。*
