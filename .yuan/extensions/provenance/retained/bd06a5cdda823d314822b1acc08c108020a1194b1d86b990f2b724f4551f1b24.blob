### 反作弊检查（反游戏化，引用 .yuan/rules/test-integrity.md）

Tester 的 🟡 Hard Gate 全绿**只有在确认没被作弊的前提下才可信**。在「物理真值审查」之外，必须并行完成下列反作弊检查，任一命中视为 🔴 Blocker：

1. **完整性差异**：单独审视本次对测试与门禁配置的改动——断言是否被删/弱化？是否新增 skip/xfail？是否动框架/阈值/豁免？实现 diff 与测试 diff 必须分离呈现、分别评审。
2. **留出集判定**：核心验收除可见用例外，须有一组作者不可见的输入/期望用于最终判定；实现不得特判可见用例。
3. **确定性校验**：核心验收由一道实现/测试作者改不动的确定性检查把关（独立 runner / 固定输入→固定期望 / 产物级断言）。
4. **独立复核**：验证由不写该实现的视角执行，给出 pass/fail + 证据（见 verdict-protocol.md）；"是否在解决问题而非绕过验证"为唯一判据。

> 判定原则：只要"通过"是靠改松验证而非改对代码换来的，即为作弊，等同未完成。被识破的作弊手法须沉淀为项目级硬约束。

- ❌ 修改业务代码
- ❌ 写无意义的测试
- ❌ 测试失败不记录 Bug
- ❌ 不判断失败类型就直接退回

