## 取消规则

```mermaid
flowchart LR
    A[⏳等待] -->|可取消| C[❌取消]
    B[🟢就绪] -->|可取消| C
    D[🔨进行中] -->|可取消| C
    E[🔄返工] -->|可取消| C
    F[❌阻塞] -->|可取消| C
    G[✅完成] -->|不可取消| X[需回滚请创建新Task]
    H[✅审查通过] -->|不可取消| X
    I[✅测试通过] -->|不可取消| X
    J[✅已部署] -->|不可取消| X
```

