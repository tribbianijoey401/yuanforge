### 回退流程

```
🔨进行中 ─── 超过超时 ──→ 🟢就绪（回退，新 Agent 可领取）
                │
                ├─ 写入「故障记录」表（类型=超时，第N次）
                │
                └─ 同任务累计 ≥3 次超时 ──→ ❌阻塞
                    原因：连续超时，可能任务过大或环境异常，等待人工介入
```

