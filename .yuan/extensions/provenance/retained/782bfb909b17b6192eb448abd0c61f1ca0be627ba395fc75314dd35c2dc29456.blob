## 会话 ID 规则

**会话 ID = 会话文件夹名**。即 `YYYYMMDD-简短描述`。

| 场景 | 会话 ID | 示例 |
|------|---------|------|
| 首次会话 | 文件夹名 | `20260714-用户认证` |
| 跨会话恢复 | 同一文件夹名 | `20260714-用户认证`（延续上次） |
| 新 Feature | 新文件夹 | `20260715-支付集成` |
| 事件日志 | `session` 字段 = 文件夹名 | `"session": "20260714-用户认证"` |
| 会话日志表 | `会话ID` 列 = 文件夹名 | `20260714-用户认证` |

> **一致性**：SESSION_LOG.md、TASK_BOARD.md、所有 Event 的 `session` 字段、PROGRESS.md 会话日志表的 `会话ID` 列，全部使用同一个值——文件夹名。

---

