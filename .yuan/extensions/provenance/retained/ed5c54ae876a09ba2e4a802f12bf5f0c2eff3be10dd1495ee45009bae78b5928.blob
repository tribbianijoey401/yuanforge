### RBAC + 租户隔离

```
全局角色（跨租户）
├── platform_admin  -- 平台管理员
└── platform_viewer -- 平台观察者

租户角色（租户内）
├── owner    -- 所有者（唯一，可转让）
├── admin    -- 管理员（邀请成员、配置）
├── member   -- 成员（正常使用）
└── viewer   -- 观察者（只读）
```

