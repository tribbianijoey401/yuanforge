### 被动退出通知机制

cronjob 触发退出协议后，用户不在场，需要通过以下方式通知：

| 触发方式 | 通知机制 | 说明 |
|---------|---------|------|
| 空闲检测（cronjob） | 发送消息到用户的主频道 | 使用 `send_message(action='list')` 查找用户最近活跃的频道，发送「会话已保存进度」 |
| 平台中断（gateway crash） | 下次用户发消息时恢复 | 用户下次发送消息 → AGENTS.md 检测到 PROGRESS.md → 自动显示上次状态 |

**实现方式**：

```
cronjob 退出协议:
    1. 扫描活跃会话（session_search 或读取 docs/ 目录）
    2. 对每个有未完成任务的会话：
       a. 执行退出协议（读 TASK_BOARD → 更新会话日志 → 写 SESSION_EXITED）
       b. 发送通知到用户主频道
          消息内容: "检测到您有未完成任务。已保存进度：[Phase] [完成] [遗留]"
    3. 如果没有活跃会话 → 静默退出
```

> **关键**：cronjob 触发退出协议时，用户可能不在线。通知消息应该发送到用户最近活跃的频道（QQ/Telegram/Discord 等），如果找不到活跃频道则仅保存不通知。
