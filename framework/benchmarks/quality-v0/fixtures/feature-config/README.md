# Feature fixture: configuration timeout

This directory is the initial Repository for `../../tasks/feature.md`. Copy it to an empty work directory for every arm, then run:

```powershell
python -m unittest discover -s tests -v
```

Baseline passes before the requested behavior is implemented. The task and its acceptance criteria—not this baseline alone—define the required new tests.
